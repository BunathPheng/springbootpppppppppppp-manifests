---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springbootpppppppppppp                  # Ingress name
  labels:
    app: springbootpppppppppppp
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springbootpppppppppppp.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springbootpppppppppppp               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
